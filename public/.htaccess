<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /LegacyNexus/

  # Keep existing files/directories reachable directly
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  # SPA fallback
  RewriteRule ^ index.html [L]
</IfModule>
